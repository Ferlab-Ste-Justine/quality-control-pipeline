# mosdepth_d4 Docker image

This folder contains a Dockerfile that installs the pre-compiled `mosdepth_d4` binary from the GitHub release `v0.3.12`.

Build:

```bash
docker build -t femgeo00/mosdepth_d4:0.3.12 containers/mosdepth
```
